{
  "name": "c_1-todo",
  "version": "1.0.0",
  "description": "Kevin Martinez ToDo list written with vanilla JS",
  "main": "index.js",
  "scripts": {
    "scss": "node-sass -w --output-style compressed -o dist/css scss/",
    "autoprefixer": "postcss -u autoprefixer -r dist/css/*",
    "uglify": "mkdir -p dist/js && uglifyjs js/*.js -m -o dist/js/app.js && uglifyjs js/*.js -m -c -o dist/js/app.min.js"
  },
  "keywords": [
    "javascript",
    "css",
    "html"
  ],
  "author": "Kevin Martinez",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.13.0",
    "node-sass": "^3.8.0",
    "postcss": "^5.0.21",
    "uglify-js": "^2.6.4"
  }
}
